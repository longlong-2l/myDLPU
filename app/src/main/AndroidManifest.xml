<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.surpassli.www.myapp">

    <!-- <uses-sdk android:minSdkVersion="4"></uses-sdk> -->
    <!--
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    -->


    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读取权限，用户写入离线定位数据 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/dlpu_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Account.School_Roll_Activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Account.Course_Result_Activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.More.About.About_us_activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.More.About.MyAppIntroduceActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Account.Level_Grade_Activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Account.Course_Table_Activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.More.Weather.WeatherActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.TestActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Education.NewsWebView"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Base.BaseToolBarActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.Account.ChangedPassWord_Activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.More.BaiduMap.LocationActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.More.About.LicenseActivity"
            android:launchMode="singleTask" />
        <!--
        <receiver android:name=".MainActivity$MyBroadcastReceiver">
            <intent-filter>
                <action android:name="tab" />
            </intent-filter>
        </receiver>
        -->
        <!-- dlpuAppKey -->
        <meta-data
            android:name="dlpu"
            android:value="58773045766613068b000585" />

        <!-- 友盟AppKey -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="58773045766613068b000585" />

        <!-- 友盟AppKey -->
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="${UMENG_CHANNEL_VALUE}" />

        <!-- 百度SDK -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="1QbDCN1EIgfMEW25WTAZyOqNgnCyZO97" />

        <service
            android:name=".service.BingUpdateService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.baidu.location.service_v2.2" />
            </intent-filter>
        </service>

        <!--
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.baidu.location.service_v2.2" >
                </action>
            </intent-filter>
        </service>
        -->

        <activity android:name=".ui.More.Scenery.SceneryActivity" />
        <activity android:name=".ui.More.ExerciseYard.ExerciseYardActivity"></activity>
    </application>

</manifest>